export enum PartnerType {
  NEWBIE = 0,
  AMBASSADOR = 1,
  ROOKIE = 2,
  DEGEN = 3,
  PRO = 4,
  ELITE = 5,
  LEGEND = 6,
}

export enum VolumeType {
  OPEN = 'OPEN',
  CLOSE = 'CLOSE',
}

export const UPGRADE_TIER_QUEUE_NAME = 'upgrade_tier';
export const RESET_USER_TIER_QUEUE_NAME = 'reset_tier';

export enum AccountType {
  NORMAL = 0,
  BOT = 1,
}
